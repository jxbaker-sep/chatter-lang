use BigInt, bigint_to_string from "std:bigint"

# Three-digit value 123 — no comma needed.
# Digits stored least-significant-first, so 123 = [3, 2, 1].
constant n is make BigInt with Digits list of 3, 2, 1
bigint_to_string n
say it
