use BigInt, bigint_add, bigint_to_string from "std:bigint"

# 1234 + 0 = 1234 — adding zero is the identity.
constant a is make BigInt with Digits list of 4, 3, 2, 1
constant b is make BigInt with Digits list of 0
bigint_add a to b
bigint_to_string it
say it
