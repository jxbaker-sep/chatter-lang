use bigint_from_number, bigint_add from "std:bigint"

# Adding a value to itself: 50 + 50 = 100. Same struct passed for both args.
bigint_from_number 50
constant fifty is it
bigint_add fifty to fifty
say it
