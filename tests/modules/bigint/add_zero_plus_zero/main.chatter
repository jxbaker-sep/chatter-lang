use bigint_from_number, bigint_add, bigint_to_string from "std:bigint"

# 0 + 0 = 0.
bigint_from_number 0
constant a is it
bigint_from_number 0
constant b is it
bigint_add a to b
bigint_to_string it
say it
