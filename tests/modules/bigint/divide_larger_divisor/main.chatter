use bigint_from_number, bigint_divide, bigint_to_string from "std:bigint"

# 5 / 100 = 0 (divisor larger than dividend)
bigint_from_number 5
constant a is it
bigint_from_number 100
constant b is it
bigint_divide a by b
bigint_to_string it
say it
