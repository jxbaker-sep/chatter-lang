use bigint_from_number, bigint_add, bigint_to_string from "std:bigint"

# 999 + 1 = 1,000 — carry propagates through all three columns and grows
# the result by one digit.
bigint_from_number 999
constant a is it
bigint_from_number 1
constant b is it
bigint_add a to b
bigint_to_string it
say it
