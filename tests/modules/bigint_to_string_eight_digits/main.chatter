use BigInt, bigint_to_string from "std:bigint"

# Eight-digit value 12,345,678 — full 3-digit groups bookended by a 2-digit lead.
constant n is make BigInt with Digits list of 8, 7, 6, 5, 4, 3, 2, 1
bigint_to_string n
say it
