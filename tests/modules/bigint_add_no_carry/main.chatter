use bigint_from_number, bigint_add, bigint_to_string from "std:bigint"

# 12 + 7 = 19 — no carry on any column.
bigint_from_number 12
constant a is it
bigint_from_number 7
constant b is it
bigint_add a to b
bigint_to_string it
say it
