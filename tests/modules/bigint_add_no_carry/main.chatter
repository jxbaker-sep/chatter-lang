use BigInt, bigint_add, bigint_to_string from "std:bigint"

# 12 + 7 = 19 — no carry on any column.
constant a is make BigInt with Digits list of 2, 1
constant b is make BigInt with Digits list of 7
bigint_add a to b
bigint_to_string it
say it
